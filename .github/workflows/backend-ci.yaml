name: Backend CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'yarn'
          cache-dependency-path: api/yarn.lock  # Pointing to the backend yarn.lock

      - name: Install dependencies
        run: |
          cd api
          yarn install

      - name: Run tests (if available)
        run: |
          cd api
          yarn test

      - name: Lint code
        run: |
          cd api
          yarn lint
